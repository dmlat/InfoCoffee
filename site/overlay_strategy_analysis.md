# Анализ стратегии глобального Overlay для мокапа

В данном документе разобран переход от индивидуального изменения свойств элементов (scale, opacity) к использованию единого глобального слоя (Overlay) с «прорезью» (masking). Проанализированы причины возникновения фризов при работе с вложенными элементами и предложены пути оптимизации.

---

## 1. Анализ расчета координат (Layout Thrashing)

Использование `getBoundingClientRect()` для определения координат «прорези» — стандартный подход, однако он таит в себе ловушку производительности, известную как **Layout Thrashing** (инвалидация макета).

### Проблема вложенности (Hero -> HeroGrid)
Когда мышь переходит с родителя (`.hero`) на ребенка (`.heroGrid`), происходит следующее:
1. **Всплытие событий:** Браузер генерирует цепочку событий (`mouseleave` на родителе, `mouseenter` на ребенке).
2. **Синхронный расчет:** Если в обработчиках этих событий вызывается `getBoundingClientRect()`, браузер обязан немедленно вычислить точную геометрию.
3. **Рекурсивная зависимость:** Хотя сам метод не рекурсивен, наличие у родителя сложных стилей (`border-radius`, `filter`, `transform`) заставляет GPU пересчитывать композитные слои, чтобы выдать корректные экранные координаты ребенка.

**Итог:** Два вызова `getBoundingClientRect()` в одном кадре при переходе между вложенными элементами создают микро-фриз, так как основной поток (Main Thread) блокируется расчетами.

---

## 2. Сравнение техник маскирования

Для создания «прорези» в Overlay можно использовать три основных метода. Оценка их производительности для GPU:

| Метод | Производительность | Плюсы | Минусы |
| :--- | :--- | :--- | :--- |
| **Box-shadow (Inverted)** | 🟢 **Высокая** | Самый дешевый метод. Не создает новых слоев. | Только прямоугольники. Сложно анимировать переход. |
| **Clip-path (Polygon)** | 🟡 **Средняя** | Аппаратное ускорение. Любые формы. | Пересчет геометрии пути при изменении координат. |
| **Mask-image (Gradient)** | 🔴 **Низкая** | Мягкие края, любые градиенты. | Растеризация маски на каждом кадре анимации. |

**Рекомендация:** Использовать `clip-path` с передачей координат через **CSS-переменные**. Это позволяет GPU обрабатывать анимацию перехода, не нагружая Main Thread.

---

## 3. Гипотеза "Nested Freeze" (Почему дети сложнее соседей)

Системе сложнее «вырезать» `heroGrid` (ребенка), чем независимый блок, по нескольким причинам:

1. **Stacking Context (Контекст наложения):** Если у `.hero` задан `filter`, `opacity` или `transform`, он создает изолированный слой отрисовки. Координаты ребенка внутри этого слоя становятся относительными, и браузеру требуется дополнительная матричная трансформация, чтобы спроецировать их на глобальный Overlay.
2. **Visual Inheritance:** При наведении на ребенка, родитель часто меняет свое состояние (например, убирает свой `hover`). Это вызывает каскадное обновление стилей во всем поддереве, что совпадает по времени с расчетом маски.
3. **Layout Dependency:** Вложенные элементы физически зависят от размеров родителя. Любое микродвижение, вызванное изменением стилей родителя при потере фокуса, заставляет систему пересчитывать всю цепочку вложенности.

---

## 4. Оптимизированный Координатор (Решение)

Для плавного переключения без фризов необходимо:
1. Использовать `requestAnimationFrame` (rAF) для дебаунсинга расчетов.
2. Использовать CSS-переменные для минимизации манипуляций с DOM.
3. Использовать `transition` в CSS для визуальной плавности.

### JavaScript (Координатор)
```javascript
const overlay = document.querySelector('.global-overlay');
let rafId = null;

function updateMask(target) {
  if (rafId) cancelAnimationFrame(rafId);

  rafId = requestAnimationFrame(() => {
    const rect = target.getBoundingClientRect();
    
    // Обновляем только переменные, не меняя структуру DOM
    overlay.style.setProperty('--m-top', `${rect.top}px`);
    overlay.style.setProperty('--m-left', `${rect.left}px`);
    overlay.style.setProperty('--m-w', `${rect.width}px`);
    overlay.style.setProperty('--m-h', `${rect.height}px`);
  });
}
```

### CSS (Overlay)
```css
.global-overlay {
  --m-top: 0px; --m-left: 0px; --m-w: 0px; --m-h: 0px;
  
  position: fixed;
  inset: 0;
  background: rgba(0, 0, 0, 0.7);
  z-index: 9999;
  pointer-events: none;
  
  /* Инвертированный многоугольник (прорезь) */
  clip-path: polygon(
    0% 0%, 0% 100%, 
    var(--m-left) 100%, 
    var(--m-left) var(--m-top), 
    calc(var(--m-left) + var(--m-w)) var(--m-top), 
    calc(var(--m-left) + var(--m-w)) calc(var(--m-top) + var(--m-h)), 
    var(--m-left) calc(var(--m-top) + var(--m-h)), 
    var(--m-left) 100%, 
    100% 100%, 100% 0%
  );
  
  transition: clip-path 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  will-change: clip-path;
}
```

---
*Документ подготовлен для оптимизации производительности мокапа InfoCoffe.*
